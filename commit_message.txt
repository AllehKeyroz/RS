fix: Corrigido ReferenceError: storeBearerToken is not defined

A função `storeBearerToken` foi renomeada para `saveBearerToken` em `src/lib/persistence.ts`.
Esta correção atualiza a chamada da função em `src/app/page.tsx` para `saveBearerToken`,
resolvendo o erro de referência.